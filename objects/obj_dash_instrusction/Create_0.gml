/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
// Definir a posição do texto para ficar acima do personagem
if (instance_exists(obj_mage)) {
    x = obj_mage.x;
    y = obj_mage.y - 32; // Ajuste o valor para a altura desejada
}
display_time = 180; // Exibir por 2 segundos (60 steps = 1 segundo)