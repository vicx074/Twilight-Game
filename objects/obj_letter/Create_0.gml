// Evento Create
is_open = false;               // Inicialmente, a carta está fechada
jogador_perto = false;         // O jogador não está perto no início
sprite_index = spr_carta;      // Define o sprite da carta fechada

// Posição central da tela
centro_x = display_get_width() / 2;
centro_y = display_get_height() / 2;
