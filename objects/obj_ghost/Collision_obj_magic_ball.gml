/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
// Destrói tanto o fantasma quanto a bola de magia
// Destroi o fantasma

// Armazena a referência da bola de magia que causou a colisão
// Destroi o próprio objeto ghost
// Destroi o próprio objeto ghost
instance_destroy();

// Destroi o objeto de magia (obj_magic_ball) diretamente
with (other) {
    instance_destroy();
}
